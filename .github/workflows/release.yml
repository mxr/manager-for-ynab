name: release

on:
  push:
    tags:
    - '**'

permissions: {}

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    permissions:
      contents: write
      id-token: write
    uses: mxr/workflows/.github/workflows/pypi-release.yml@4d2a6bd2164c4dd6b9cedf67fe61be03377e5e95 # v0.1.1
